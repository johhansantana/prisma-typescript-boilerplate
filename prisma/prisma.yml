endpoint: http://localhost:4466
datamodel: datamodel.prisma

# uncomment when re-generating until bugfix
#generate:
#  - generator: typescript-client
#    output: ../src/generated/prisma

hooks:
  post-deploy:
    - prisma generate